{
    "require-dev": {
        "squizlabs/php_codesniffer": "dev-wordwrap"
    },
    "scripts": {
        "phpcs": "phpcs -s --basepath=. --report-width=99 --report=code demo.php",
        "demo": "composer run phpcs | grep -v Time: | diff -u - expected.txt"
    },
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/MatmaRex/PHP_CodeSniffer_"
        }
    ]
}
